// Tax slab calculation service (deterministic — no LLM math)
// Contains: calculateTax(grossIncome, regime, slabYear)
// LLM identifies which rule applies; this function does the arithmetic
// TODO Phase 3: implement
