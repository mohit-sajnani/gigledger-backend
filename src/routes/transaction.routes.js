// Transaction routes
// POST   /api/transactions          — create (manual entry)
// GET    /api/transactions          — list (filter by status, date, source)
// GET    /api/transactions/:id      — get one
// PUT    /api/transactions/:id      — update
// DELETE /api/transactions/:id      — soft delete
// POST   /api/transactions/bulk     — bulk import (simulated platform data)
// PATCH  /api/transactions/:id/approve — user confirms AI categorization
// TODO Phase 1: implement
